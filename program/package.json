{
  "name": "gojek-parking-lot",
  "version": "1.0.0",
  "description": "A simple command line implementation of a ticketing system for parking lot",
  "main": "index.js",
  "scripts": {
    "lint": "tsc --noEmit && eslint 'src/**/*.ts' --quiet --fix",
    "build": "tsc && chmod +x dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Munim Dibosh",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.0.2",
    "@typescript-eslint/eslint-plugin": "^1.9.0",
    "@typescript-eslint/parser": "^1.9.0",
    "eslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}